#!/bin/sh
# Placeholder status report for CatalinaPerformance.
# Future versions should collect read-only system status before any toggle behavior is implemented.

set -eu

printf '%s\n' "CatalinaPerformance status report placeholder"
printf '%s\n' "No system changes are performed by this script."
printf '%s\n' "Planned read-only checks: macOS version, Intel hardware, power source, RAM pressure, swap usage, disk space, CPU temperature, and fan speed."
